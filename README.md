# Screen-recorder
Description: A simple Bash script to install and launch SimpleScreenRecorder on Debian/Ubuntu systems. It automates system updates, installs required dependencies, starts screen recording in the background, and allows the user to stop and save the recording interactively from the terminal with colorful ASCII banners.
